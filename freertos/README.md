# NovaV1 FreeRTOS Demo Scaffold

This directory contains the NovaV1 M-mode FreeRTOS demo scaffold. It is intentionally built by repository scripts instead of a separate Makefile so the repository remains Mill-first and avoids sbt entirely.

Use:

```bash
./scripts/install_riscv_gcc.sh
./scripts/run_freertos_demo.sh
```
